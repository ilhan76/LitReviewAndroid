/*
* Hilt & kapt.
* Should be applied to every module with DI included.
*/
apply plugin: 'dagger.hilt.android.plugin'

dependencies {
    implementation di.hilt
    annotationProcessor di.hiltCompiler
}